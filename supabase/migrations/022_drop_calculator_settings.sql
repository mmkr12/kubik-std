-- ============================================================
-- KUBIK.std — удаление мёртвой старой формулы калькулятора
-- Выполнить в Supabase SQL Editor ПОСЛЕ 021
-- ============================================================
-- calculator_settings обслуживала старую упрощённую формулу лендинга
-- (lib/pricing.ts, /api/calculate) до того, как лендинг стал использовать
-- тот же калькулятор "Световые буквы на каркасе", что и заявки в ERP
-- (components/main-calculator.tsx рендерит LightLettersOnFrameCalculator
-- везде, включая mode="public"). Ни код, ни база в эту таблицу больше не
-- обращаются — удаляем вместе с RLS-политиками (снимаются автоматически).

drop table if exists calculator_settings cascade;
